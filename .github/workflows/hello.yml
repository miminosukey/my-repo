name: Hello
on: [push, pull_request]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world"
      - run: ls
      - uses: actions/checkout@v4
      - run: pwd
      - run: ls
